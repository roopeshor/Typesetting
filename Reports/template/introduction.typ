= Introduction <sec:intro>
== Sub section
goto `settings.typ` to change how things are displayed.
The file also contains report title. Change `report-title` variable in `settings.typ` to change title of the report. This will be reflected in title page, certificate as well as in headers.


The table of contents, and all sorts of lists are in `tocloft.typ`.

This secton covers some basic constructs. usually there is no header in chapter, reference, and appendix start pages. This is default in many typesetting world

*Best practice is to split all chapters into separate `.typ` files, and then include it in `report.typ` main file. This improves maintainability.*